<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="mas.com.filmLib">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
            android:allowBackup="true"
            android:icon="@drawable/film"
            android:label="@string/app_name"
            android:roundIcon="@drawable/film"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:name="mas.com.filmLib.application.TranslatorApp">

        <activity
                android:name="mas.com.filmLib.view.main.MainActivity"
                android:screenOrientation="fullSensor"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
                android:name="mas.com.filmLib.view.descriptionscreen.DescriptionActivity"
                android:screenOrientation="fullSensor" />
    </application>

</manifest>
